Nightly Jules orchestration triage for imgsnag (~/Developer/projects/imgsnag). Run the established review cycle autonomously:

1. SYNC: git fetch --prune; gh pr list --state open; list Jules sessions updated since the last triage (curl https://jules.googleapis.com/v1alpha/sessions?pageSize=25 with header X-Goog-Api-Key from `security find-generic-password -s jules-api-key -w` — never print the key).
2. TRIAGE every open PR/new branch vs origin/main with `git merge-tree --write-tree` (NOOP / NEW / CONFLICT; ignore dist/ paths when judging novelty).
3. REVIEW the NEW ones on the merits (read the diffs). Merge accepted work by cherry-pick: exclude dist/ from every pick (git restore --staged dist; git checkout -- dist), preserve google-labs-jules[bot] authorship, keep .jules/ journal entries but CORRECT their dates (agents chronically misdate), resolve locale-file conflicts by JSON union-merge and VALIDATE with python3 -m json.tool before committing. For duplicate clusters: oldest equivalent wins, best wins if they differ. Never weaken security invariants (resolveUrl allowlist, urlObj.href normalization, e.isTrusted, download_svg fail-closed validation); reject cold-path micro-opts with correctness risk. A branch based on a stale main that conflicts broadly: extract its unique contribution and apply that to current main rather than fighting the conflicts.
4. VERIFY: npm test must be green (all suites) and node --check on touched src files before pushing. Rebuild dist once at the end (bash build.sh) and commit it separately. Optionally run `node tools/e2e-smoke.mjs` for an end-to-end check.
5. CLOSE the rest with explanatory comments and --delete-branch: duplicates (cite the merged PR), no-op PRs (per AGENTS.md a no-op run must not open a PR), superseded iterations, and outright rejections (explain WHY, with the measurement or reasoning). If Prospector filed an RFC as a PR, transplant the content into an rfc-labeled GitHub issue, then close the PR citing the issue.
6. SESSIONS: for each AWAITING_USER_FEEDBACK session, read its latest agentMessage and reply via :sendMessage (inline curl --data-raw; the harness loop-with-variable pattern 404s). Stand down duplicates/stale-snapshot claims (tell them the landing commit), demand quoted file:line evidence from Warden findings, approve reasonable plans that match the persona's charter, and block any attempt to add npm dependencies.
7. BOUNDARIES: never bump versions, never tag, never touch the stores, never force-push, never delete branches that contain unreviewed unique work — leave anything genuinely ambiguous open with a comment addressed to Maxime.
8. REPORT: end with a compact scoreboard (merged/closed/stood-down/tests count) and flag anything needing Maxime's decision.
